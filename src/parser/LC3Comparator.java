package parser;

public class LC3Comparator implements Comparator {

    public boolean isCommand(String text) {
            return false;
    }

    public boolean isComment(String text) {
        return false;
    }
}
